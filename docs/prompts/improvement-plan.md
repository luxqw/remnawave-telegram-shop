# План улучшений: CI/CD, git-теги, Telegram-меню, admin-панель

> Оптимизированный промт (см. `/ecc:prompt-optimizer`). Скопируйте содержимое ниже в новую
> сессию, чтобы выполнить работу.

```
Проект: remnawave-tg-shop-bot (Go 1.25, Telegram-бот) + web/admin (Preact+TS+Vite, отдаётся
через internal/webapp/*.go). CI: .github/workflows/docker-image.yml (build/vet/test/typecheck,
docker push по тегу через docker/metadata-action).

Работа состоит из трёх фаз в рамках ОДНОГО PR. Используй blueprint skill, чтобы сначала
построить план на все три фазы с порядком выполнения, затем выполняй фазы последовательно,
с /verify после каждой (`go build ./...`, `go vet ./...`, `go test ./...`,
`cd web/admin && npm run build`).

── ФАЗА A: репозиторий и CI/CD ──
- Добавь golangci-lint шаг в CI (сейчас только build/vet/test — нет линтера).
- Добавь .github/dependabot.yml для go modules и web/admin npm.
- Добавь issue/PR templates в .github/ (сейчас только FUNDING.yml).
- Если в репозитории нет LICENSE — спроси пользователя, какую лицензию добавить, не выбирай
  сам молча.
- Унификация git-тегов: закрепи конвенцию — ВСЕ НОВЫЕ теги строго `vX.Y.Z` (в репозитории сейчас
  46 тегов вида v1.0.x и один голый "2.0.3" без префикса). НЕ трогай, не удаляй и не
  перезаписывай существующие 47 тегов — это история релизов. Задокументируй схему в
  CONTRIBUTING.md или в разделе README про релизы.
- Пройдись по readme.md (353 строки) и актуализируй его под текущий функционал (device slots,
  admin webapp, referral, Tribute/Rollypay платежи, sync) — убери устаревшее, добавь раздел
  про запуск/деплой admin-панели, добавь CI-бейдж.

── ФАЗА B: редизайн Telegram-меню бота ──
Проблема (зафиксирована по скриншотам меню слотов/устройств, internal/handler/devices_handler.go
и смежные хендлеры): плотный текст-абзац над кнопками, отдельная кнопка "Удалить #N — <модель>"
на каждое устройство (не масштабируется при росте числа устройств), неоднозначная формулировка
для необратимых действий ("Отменить — оставить 0 слот(ов)" читается как отмена, а по факту
подтверждает уменьшение слотов).

Задача: пройтись по ВСЕМ экранам меню бота в internal/handler/ (не только устройства) и привести
к единому стилю:
- короткая статус-строка + компактный инфо-блок вместо стены текста
- списки (устройства и т.п.) — через один инлайн-список с callback на строку или экран
  "выбери → действие", а не кнопка-на-каждый-item
- единая, недвусмысленная формулировка для деструктивных действий (явно отличать от безопасных)
- посмотри, какая конвенция уже начата в недавних коммитах (bf85322, 1c9a39f — "unify devices
  menu") и распространи её на остальные меню вместо новой с нуля.

── ФАЗА C: полная переработка admin-панели (web/admin) ──
Решение: полный редизайн/переписывание фронтенда с нуля (не точечные патчи), т.к. проблема
("работает либо плохо, либо никак") не имеет конкретики для точечного фикса.

Перед переписыванием — зафиксируй текущий функциональный охват, чтобы rewrite его не потерял:
существующие routes (Dashboard, Users, UserDetail, Orders, Referrals, Broadcast, Notifications,
Webhooks, System, AuditLog, Activity) и backend-контракты, которые они дёргают
(internal/webapp/handlers_*.go).

- Backend-контракты internal/webapp/*.go не менять, если конкретный баг не потребует —
  переработка фокусируется на фронтенде.
- Auth/browser-link доступ (добавлен в 40b3407) — обязательно прогнать через
  security-reviewer agent, это auth-чувствительный путь.
- Дизайн: следуй web design-quality стандартам — без generic dashboard-by-numbers вида,
  реальная иерархия, состояния hover/focus/loading/empty/error.
- Это UI-задача: обязательно подними dev server (`npm run dev` в web/admin) и пройди каждый
  route в браузере до заявления о готовности — так же проверь оба состояния, если тема
  переключаемая.

── Границы (что НЕ делать) ──
- Не трогать интеграционную логику Remnawave/Tribute/Rollypay платежей, если это не требуется
  напрямую для ФАЗЫ C.
- Не удалять и не перезаписывать существующие 47 git-тегов.
- Не добавлять тестовый фреймворк в web/admin (сейчас только Vite+TS, без раннера) без явного
  запроса — если ФАЗЕ C нужны UI-тесты, сначала спроси.
- Не выбирать LICENSE молча.

── Критерии приёмки ──
- CI зелёный: go build/vet/test, `npm run build` в web/admin, docker workflow не сломан.
- Новые теги — только `vX.Y.Z`, существующие теги нетронуты.
- Все меню бота в internal/handler/ приведены к единому стилю без кнопки-на-item списков.
- Admin-панель переписана, все текущие routes доступны с не меньшим функционалом, чем сейчас,
  проверено вручную в браузере (dev server), не только typecheck/build.
- /code-review и security-reviewer agent пройдены без CRITICAL/HIGH проблем на auth/payment-
  смежном коде.
```

## Короткая версия

```
Используй blueprint skill для плана на 3 фазы (CI/теги/readme → редизайн Telegram-меню →
полный rewrite web/admin). Фаза A: golangci-lint + dependabot + issue templates + только новые
теги vX.Y.Z (историю не трогать) + актуализация readme. Фаза B: единый стиль для всех меню
internal/handler/ (проблема видна в devices_handler.go — стена текста + кнопка-на-device,
неоднозначные формулировки удаления). Фаза C: полный rewrite web/admin (Preact+TS), backend
internal/webapp/*.go не трогать без нужды, auth через security-reviewer, проверить в браузере
дев-сервером. /verify после каждой фазы. Не трогать payment-интеграции и историю тегов.
```
